[project]
name = "constat-aurora-eol"
version = "0.0.0"
description = "Insight: Aurora MySQL/PostgreSQL Extended Support (gap chiffré)"
requires-python = ">=3.13"
dependencies = [
    "constat-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/constat_aurora_eol"]
